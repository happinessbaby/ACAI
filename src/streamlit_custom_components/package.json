{
    "name": "streamlit_custom_components",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "login": "cd login/frontend && npm run start",
        "lightbox": "cd lightbox/frontend && npm run start",
        "start": "npx concurrently \"npm run login\" \"npm run lightbox\" "
    },
    "devDependencies": {
        "concurrently": "^1.0.0"
    }
}
